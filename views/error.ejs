<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>System Error - SwingCity</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <script src="https://cdn.jsdelivr.net/npm/iconify-icon@2.3.0/dist/iconify-icon.min.js"></script>
    <style>
        iconify-icon { vertical-align: -0.125em; }
        .error-icon iconify-icon { font-size: 3rem; }
    </style>
</head>
<body>

<div class="error-container">
    <div class="error-content">
        <div class="alert alert-danger">
            <div class="error-icon"><iconify-icon icon="ph:warning-bold" style="color: #f59e0b;"></iconify-icon></div>
            <h1 class="mb-3">System Error</h1>
            <p class="lead">We're sorry, but there was an error processing your request.</p>
            <% if (error && error.message) { %>
                <div class="error-details">
                    <h4>Error Details:</h4>
                    <pre><%= error.message %></pre>
                    <% if (error.stack && process.env.NODE_ENV === 'development') { %>
                        <details class="mt-3">
                            <summary>Stack Trace (Development)</summary>
                            <pre class="text-muted small"><%= error.stack %></pre>
                        </details>
                    <% } %>
                </div>
            <% } %>
        </div>
        
        <div class="error-actions">
            <a href="/" class="btn btn-primary">
                <iconify-icon icon="ph:house-bold"></iconify-icon> Return to Home
            </a>
            <a href="/teams" class="btn btn-secondary">
                <iconify-icon icon="ph:users-three-bold"></iconify-icon> Team Manager
            </a>
            <button onclick="window.location.reload()" class="btn btn-outline-primary">
                <iconify-icon icon="ph:arrows-clockwise-bold"></iconify-icon> Try Again
            </button>
        </div>
        
        <div class="error-help">
            <h4>What can you do?</h4>
            <ul>
                <li>Check your internet connection</li>
                <li>Refresh the page and try again</li>
                <li>Contact the system administrator if the problem persists</li>
                <li>Return to the home page and start over</li>
            </ul>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
